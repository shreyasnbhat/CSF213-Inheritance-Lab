/**
 *  An interface that signifies if an item contributes to cost or not.
 */
public interface IChargeable
{
    /**
     * Compute cost of a chargeable item.
     */
    /**
     * @return cost of a chargeable item.
     */
    int getCost();
}
